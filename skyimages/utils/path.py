def file_exists(root_dir: str) -> bool:
    """Checks if the file exists

    Parameters
    ----------
    root_dir : str
        Relative or absolute path

    Returns
    -------
    bool
        True if the folder exists
    """